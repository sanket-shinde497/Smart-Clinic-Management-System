# NOTE: This file is left for reference only. Active datasource settings are in src/main/resources/application.properties
# The lines below are commented out to avoid conflicting datasource config during local dev.
#spring.datasource.url=jdbc:mysql://172.21.227.35:3306/demo
#spring.datasource.username=root
#spring.datasource.password=root

# Optional good practice
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Disable Mongo auto-configuration (remove if you actually need Mongo at runtime)
# This prevents Spring Boot from trying to create a MongoClient when no Mongo is available.
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,org.springframework.boot.autoconfigure.mongo.MongoReactiveAutoConfiguration

# API base path default for reference (active config is in src/main/resources/application.properties)
api.path=/
